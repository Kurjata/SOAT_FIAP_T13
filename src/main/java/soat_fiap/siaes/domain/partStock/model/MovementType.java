package soat_fiap.siaes.domain.partStock.model;

public enum MovementType {

    ENTRADA, //entrada fornecimento
    SAIDA_OS, //saída de produtos via OS
    DEVOLUCAO_OS, //entrada de produtos via OS
    AJUSTE, //ajustes a possível erro de estoque
   }
